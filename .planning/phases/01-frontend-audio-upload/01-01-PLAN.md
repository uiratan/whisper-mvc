---
phase: 01-frontend-audio-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/page.tsx
  - src/app/layout.tsx
  - tailwind.config.ts
  - postcss.config.mjs
autonomous: true

must_haves:
  truths:
    - "Next.js development server runs without errors"
    - "TypeScript compilation succeeds"
    - "Home page renders in browser"
  artifacts:
    - path: "package.json"
      provides: "Next.js and TypeScript dependencies"
      contains: "next"
    - path: "src/app/page.tsx"
      provides: "Main page component"
      min_lines: 10
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "compilerOptions"
  key_links:
    - from: "src/app/page.tsx"
      to: "Next.js App Router"
      via: "default export React component"
      pattern: "export default function"
---

<objective>
Initialize Next.js project with TypeScript and Tailwind CSS configuration.

Purpose: Establish the foundation for the audio upload web application with modern tooling.
Output: Running Next.js development environment ready for feature implementation.
</objective>

<execution_context>
@/home/uira/.claude/get-shit-done/workflows/execute-plan.md
@/home/uira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with TypeScript and Tailwind</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    src/app/page.tsx
    src/app/layout.tsx
    tailwind.config.ts
    postcss.config.mjs
    .gitignore
  </files>
  <action>
Create Next.js project using create-next-app with the following configuration:
- App Router (not pages directory)
- TypeScript enabled
- Tailwind CSS included
- ESLint included
- No src/ directory initially (create-next-app default is root app/)

After creation, verify the project structure and clean up the default page to prepare for upload UI:
- Remove boilerplate content from page.tsx
- Keep layout.tsx with basic metadata
- Ensure Tailwind is properly configured
- Add .next and node_modules to .gitignore if not present

Use command: `npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src --import-alias "@/*"`

Note: The command will prompt for confirmation to initialize in non-empty directory. Approve it.
  </action>
  <verify>
Run development server and verify it starts without errors:
```bash
npm run dev
```
Server should start on http://localhost:3000

Check TypeScript compilation:
```bash
npx tsc --noEmit
```
Should complete with no errors.
  </verify>
  <done>
- package.json exists with next, react, typescript dependencies
- Development server starts successfully on port 3000
- TypeScript compiles without errors
- Home page accessible at http://localhost:3000
  </done>
</task>

<task type="auto">
  <name>Create basic page layout for audio upload</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Move app/ directory to src/app/ for better organization (optional but recommended pattern).

Then create a clean, minimal home page in src/app/page.tsx with:
- Centered layout using Tailwind CSS
- Page title "Audio Transcription Test"
- Container for upload component (placeholder div with descriptive text)
- Simple, clean styling with gradient background

Do NOT implement the upload component yet — just create the page shell that will contain it. Use Tailwind classes for styling.

Example structure:
```tsx
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-indigo-100">
      <div className="max-w-2xl w-full p-8">
        <h1 className="text-4xl font-bold text-center mb-8 text-gray-800">
          Audio Transcription Test
        </h1>
        {/* Upload component will go here */}
        <div className="bg-white rounded-lg shadow-lg p-6">
          <p className="text-gray-600 text-center">Upload component placeholder</p>
        </div>
      </div>
    </main>
  )
}
```
  </action>
  <verify>
Visit http://localhost:3000 and confirm:
- Page displays centered title "Audio Transcription Test"
- Background has gradient styling
- White card/container visible for future upload component
- No console errors in browser dev tools
  </verify>
  <done>
- src/app/page.tsx contains clean page layout
- Page renders without errors
- Styling applied correctly with Tailwind
- Ready for upload component integration
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Development server runs without errors: `npm run dev`
2. TypeScript compilation succeeds: `npx tsc --noEmit`
3. Visit http://localhost:3000 and verify home page displays
4. Check browser console for any errors (should be clean)
5. Verify project structure follows Next.js App Router conventions
</verification>

<success_criteria>
- Next.js project initialized and configured
- TypeScript and Tailwind CSS working correctly
- Development server starts and serves pages without errors
- Home page displays with basic layout ready for upload component
- No compilation or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-audio-upload/01-01-SUMMARY.md`
</output>
